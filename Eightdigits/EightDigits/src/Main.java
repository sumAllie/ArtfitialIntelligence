import ASearch.ASearch;
import AStar.AStar;
import Bound.Bound;
import Depth.Depth;
import MountClimb.MountClimb;
import Width.Width;

public class Main {
    public static void main(String[] args) {
        //new Width().resolve();
        //new Depth().resolve();
        //new Bound().resolve();
        //new MountClimb().resolve();
        new ASearch().resolve();
        //new AStar().resolve();
    }
}

